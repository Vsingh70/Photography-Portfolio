{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities granted to the main window. Includes OAuth + Drive upload commands. Remote URLs whitelist vflics.com so the deployed Studio page can call invoke().",
  "windows": ["main"],
  "remote": {
    "urls": ["https://vflics.com/*"]
  },
  "permissions": [
    "core:default",
    "core:window:default",
    "core:webview:default",
    "allow-start-oauth",
    "allow-signed-in-email",
    "allow-sign-out",
    "allow-upload-to-drive"
  ]
}
